{
  // include : 컴파일을 진행할 폴더들을 지정할 수 있다.
  "include": ["./*"],

  // ./ : 현재 폴더의 모든 파일의 경로, 즉 현재 폴더에 있는 모든 파일을 컴파일 대상으로 지정
  // /** : 폴더
  // /* : 파일

  // ex) src/**/* : src 폴더의 하위 폴더와 폴더 안에 있는 모든 파일의 경로 지정
  // exclude : 컴파일을 하지 않을 폴더를 지정할 수 있다.
  "exclude": [],
  // compilerOptions : typescript 파일을 컴파일할 시 어떤 옵션으로 컴파일을 진행할지 속성 정의
  "compilerOptions": {
    // 컴파일된 파일을 내보낼 경로 지정
    "outDir": "./dist"
  }
}

// package.json에 추가
// "scripts" : {
//   "build" : "tsc"
// }

// tsc 명령어는 typescript 컴파일을 실행하는 명령어
// typescript 코드를 tsconfig.jon 파일 설정값을 가지고 자바스크립트